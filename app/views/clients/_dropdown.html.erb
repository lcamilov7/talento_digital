<div class="dropdown">
  <button class=" btn btn-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
    Status
  </button>
  <div class="dropdown-menu">
    <%= link_to "Todos", clients_path, class: "dropdown-item" %>
    <%# cuando recorro un hash el primr argumento es el key y el segundo key values %>
    <% Client.statuses.each do |description, number| %>
      <%= link_to description, clients_path(query: number), class: "dropdown-item" %>
    <% end  %>
  </div>
</div>
